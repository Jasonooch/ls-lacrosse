# REQUIRED: A generated secret for Payload (used for JWT signing & session encryption)
# Generate a secure random string using: openssl rand -hex 32
# NEVER commit this value to version control - keep it secret!
PAYLOAD_SECRET=

# OPTIONAL: Override the CMS origin used for API/media URLs in all environments
# Example: https://cms-staging.example.com
NEXT_PUBLIC_CMS_ORIGIN=
